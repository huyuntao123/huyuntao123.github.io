常量：
1.176e+10 = 1.176 * 10^10		1.8e-3 = 1.8 * 10^-3

字符串 ：以 \0 作为结束符	例如 ："a" 由 'a' '\0'组成


变量： 

auto : 局部变量（未设定初值的局部变量，数值为随机值。）

register：寄存器变量（频繁使用的变量就定义为寄存器变量）

static：静态变量（默认为0，生命周期与全局变量一样——从程序开始到结束）	

extern：引用外部变量（static修饰的变量其他文件无法使用）


