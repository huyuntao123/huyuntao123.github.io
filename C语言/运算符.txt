！：逻辑反	&&：逻辑与（一个为0全为0）	||：逻辑或（一个为1全为1）	&&优先||

~：位逻辑反	&：位逻辑与（化为二进制每一位相运算）	|：位逻辑或	^：位逻辑异或（相异为真）	>>：右移位(原先最高为是什么就补什么，相当于除上2^n)	<< : 左移位（右补0，相当于乘上2^n）

++x：先自增再运算	x++：先运算再自增

，运算符：从左到右运算，最后的值是表达式最右边的	例：z = (z=1,y=2,x=3)	z = 3

sizeof(类型或变量名)：只针对数据类型，不针对变量。
